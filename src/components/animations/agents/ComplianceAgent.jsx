"use client";

import AgentAnimation from "./AgentAnimation";

const ComplianceAgent = (props) => (
  <AgentAnimation src="/animations/agents/compilance_agent.riv" {...props} />
);

export default ComplianceAgent;
