"use client";

import AgentAnimation from "./AgentAnimation";

const PacingAgent = (props) => (
  <AgentAnimation src="/animations/agents/pacing_agent.riv" {...props} />
);

export default PacingAgent;
