"use client";

import AgentAnimation from "./AgentAnimation";

const StrategyAgent = (props) => (
  <AgentAnimation src="/animations/agents/strategy_agent.riv" {...props} />
);

export default StrategyAgent;
